# 《火种》Project Ember — 开发规划

> **用途**：把简报第 12 章的阶段骨架展开成可执行的任务清单，并记录当前进度。这是每天开工时打开的那一份。
> **权威顺序**：`设计文档初稿.md` > `火种_项目开发简报.md` > **本文档** > 代码。
> 本文档不得新增或修改任何玩法规则；只负责"怎么做、做到哪了、算不算做完"。
> **起始日**：2026-09-03 ｜ **引擎**：Unity 6000.0.76f1（URP），交付前不升级。

---

## 0. 怎么用这份文档

1. **开工前**：看第 2 节，找到当前阶段第一个 ⬜ 的任务。
2. **做完一项**：把 ⬜ 改成 ✅。做不动就改成 ⚠️ 并在同行写一句为什么。
3. **发现规则冲突**：不要在这里改规则。去查初稿；初稿没写的，记进第 4 节的待定表，问完再动手。
4. **改了方案**：去 `取舍日志.md` 记一条（原方案 / 实际问题 / 当前决定 / 代价 / 后续验证）。**这是设计文档第 7 章的唯一素材来源，当场记，事后补必然丢细节。**
5. **阶段收尾**：跑一遍该阶段的验收标准，然后补设计文档对应章节。

**状态记号**：✅ 完成 ｜ 🔨 进行中 ｜ ⬜ 未开始 ｜ ⚠️ 受阻/有代价 ｜ 📄 只写文档不实现

---

## 1. 现状快照（2026-09-03）

**已存在的代码**（`Assets/Scripts/`）：

| 文件 | 职责 | 状态 |
|---|---|---|
| `Flame.cs` | 逻辑单例、四态状态机、载体引用、dyingTimer | ✅ 可跑，Oxygen 仍是硬编码 `10f` |
| `Carrier.cs` | 载体抽象基类：fuel / burnRate / 自扣 / Attach / Detach / 归 0 自毁 | ✅ |
| `Torch.cs` | 火把：顶端挂点 + 杆体随燃料缩短 + 碰撞传火 | ✅ |
| `FlameRunner.cs` | 场景侧每帧驱动 `Flame.Update`，转发熄灭事件 | ✅ |

**核心链路已通**：点火 → 火把自扣燃料 → 杆体变短 → 阈值降级 → 归 0 熄灭 → 触发失败事件；碰到另一个载体可交接。

**缺口（阶段 1 剩余）**：氧气这一整条支路（`OxygenZone`）、视觉层（`FlameVisual`）、吹火复燃。**在这三项补齐前，公理 II 在游戏里还不存在**，玩家看到的只是一根变短的棍子。

---

## 2. 阶段任务拆解

### 阶段 0 · 工程搭建（0.5 天）✅ 已完成

- ✅ Unity 版本锁定 `6000.0.76f1`、URP 配置、目录结构
- ✅ `取舍日志.md` 已开始记录
- ✅ 白模 Prefab 库 `Assets/Prefabs/Blockout/`（Cube / Capsule / Cylinder）
- ✅ **颜色规范表**：`Assets/Materials/` 六个材质 + [`材质颜色.md`](../材质颜色.md)（玩家 / 野兽 / 木 / 金属 / 水 / 环境）
  - ✅ 红橙黄白已声明保留给火焰与受热管道，普通环境不得大量使用（简报 15 章该待定项可关闭）

---

### 阶段 1 · 火焰核心（1.5 天）⭐ 最优先 🔨 进行中

**目标**：三条公理里的前两条在游戏里都能被看见、被玩坏、被救回来。

#### 1.1 状态机与载体 ✅

- ✅ `Flame` 逻辑单例（私有构造，`Instance` 编译期唯一）
- ✅ 四态 `Burning / Ember / Dying / Extinguished`，**绝对阈值**：`fuel<10`、`oxygen<6`、`fuel<5`、`oxygen<2`、`dyingDuration=8s`
- ✅ 降级判定排在恢复判定之前（否则 Ember 会在同一帧被判回 Burning）
- ✅ `Carrier` 抽象基类：`Fuel` / `Position` / `ConsumeFuel` / `OnAttach` / `OnDetach`
- ✅ **燃料由载体自己在 `Update` 里扣**，`Flame` 只读不写（初稿：fuel 保存在载体上）
- ✅ 未点燃的载体不自燃（`isLit` 闸门）——否则场上备用火把会自己烧光
- ✅ `Torch`：顶端挂点、杆体随燃料缩短、碰撞传火
- ✅ `FlameRunner`：`LateUpdate` 驱动（等所有载体这一帧扣完再判状态，避免慢一帧），并暴露 `onExtinguished` 供关卡接失败重开
- ✅ 载体自毁：fuel 归 0 → 火同帧 Extinguished → `OnDetach` 里 Destroy（销毁放在 Flame 放手之后，避免悬空引用）

#### 1.2 氧气支路 🔨 代码已完成，待场景验收

- ✅ `OxygenZone.cs`：`[SerializeField] float oxygen`，只读属性，`[RequireComponent(Collider)]` + OnValidate 检查 Is Trigger + Gizmo 可视化
- ✅ 区域切换：**由区域检测载体**（`OxygenZone.OnTriggerEnter/Exit` → `carrier.EnterZone/ExitZone`）
  - ⚠️ 与本节原计划（"载体身上的 trigger 更新 Flame 的当前区域"）相反，改的理由有二：
    1. `Torch` 已有自己的 `OnTriggerEnter`（传火用）。在 `Carrier` 基类再写一个会被子类**静默覆盖**，Unity 只调最派生的那个，不报错
    2. 火交接到新载体时，新载体早已在区域内，不会再触发 Enter，`Flame` 会攥着旧区域
  - 结果：`Torch` 一行未改；区域引用存在**载体**身上，`Flame.Oxygen => currentCarrier.Oxygen`，与 `Fuel` 完全对称，交接零同步代码
  - 区域归属**不判 `isLit`**：在哪个房间是载体的客观属性，与点没点燃无关
- ✅ 兜底改为**代码常量** `OxygenZone.DefaultOxygen = 10f`，不再要求场景最外层放大 zone
  - 理由：场景物体会被误删、会忘了放，常量不会。哪一关的"外面"不是露天（太空关外面是真空），那一关自己套一个大 zone 盖掉
  - 多区域重叠时**后进入者优先**；读取时倒序跳过已销毁的 zone（Unity 伪 null）
- ✅ `Flame.Oxygen` 的硬编码 `10f` 已替换
- ✅ 初版数值（露天 10 / 密闭 `Zone_Cave` 5 / 深处 `Zone_Deep` 1.5 / 真空 `Zone_Vacuum` 0）
- ✅ 场景装配与验收（2026-09-03 通过）
  - ✅ 进 `Zone_Cave` → `Burning → Ember`，成因"氧气不足"，`fuel` 全程不动
  - ✅ 进 `Zone_Deep` → `Ember → Dying`
  - ✅ 退回 `Zone_Cave` → `oxygen` 回到 5.0 **而非兜底值 10.0**（重叠栈成立）
  - ✅ 停在 `Zone_Deep` 不动 → 8s 后 `Extinguished`，成因"濒熄倒计时走完"
  - ✅ 进 `Zone_Vacuum` → **立刻** `Extinguished`，成因"氧气归零"，不走倒计时
- ✅ 调试可读性：状态迁移日志带**成因**（燃料 / 氧气 / 两者）与当帧两个读数；区域进出打 `[Zone]` 日志
  - 没有成因的日志分不出"因燃料"还是"因氧气"，阶段 4 调参会反复卡在这里

**验收**：走进密闭房间 → 火降级；走出来 → 恢复。全程不写任何"进出房间"的玩法事件。✅

#### 1.3 视觉层 🔨 代码已完成，待场景装配

- ✅ `FlameVisual.cs`（**初稿定名，不叫 FlameVFX**）：`LateUpdate` 只读 `Flame.Instance`，Transform 对齐 `Flame.VisualPosition`
- ✅ v1 只做**尺寸**（+ 火光 range/intensity），浑浊度与颜色留到阶段 5
- ✅ **尺寸跟绝对 `fuel` 走，映射区间 `[0, EmberFuel]`，超过一律满尺寸**
  - 简报 5.3 原写「尺寸 ← fuelRatio」，那是阈值还是比值制时定的。初稿改成绝对阈值后，比值方案会让视觉与状态机打架（容量 200 的篝火剩 15 时比值 0.075 看着奄奄一息，状态机却判 Burning）
  - 跟绝对值走则"开始变小"与"开始警戒"是同一刻，且换载体不用调参
  - 代价：10 以上看不出区别——那段信息由火把长度承担（`Torch.ApplyLength` 走比值）。**棍子长度＝总量，火苗大小＝紧迫度**
- ⬜ 场景装配（`FlameVisual` + 子物体 `Body` / `Glow`）
- ⬜ **边界自测：禁用整个 `FlameVisual`，游戏逻辑必须照常跑通。** 一旦 VFX 里出现影响判定的代码，架构就漏了

#### 1.3.2 火焰挂点：从手填魔数改为代码计算 ✅

**症状**：火苗开局悬在杆顶上方；杆体缩短时火苗下降速度约为杆顶的两倍。

**根因**：`FlamePosition` 原先挂在 `Stick` 底下靠继承缩放自动下降，其 `localPosition.y` 必须等于杆体网格的顶面高度。装配指引按 Cylinder 给了 `1.0`，实际用的是 **Cube（顶面 0.5）**——差一倍，于是两者都乘同一个 `localScale.y` 时下降速率差一倍。

**修法**（不是把 1.0 改成 0.5）：
- 从网格包围盒读 `bounds.max.y` / `bounds.min.y`（用 max/min 而非 extents，兼容包围盒不居中的美术模型）
- `ApplyLength` 用 `stick.TransformPoint(0, topLocalY, 0)` 算杆顶世界坐标，缩放与旋转由 `TransformPoint` 带上
- `flamePosition` 降级为**纯可视化 + 阶段 5 的粒子锚点**，位置由代码写入；留空也能跑
- 挂点移出 `Stick`，改挂 `Torch` 根节点——顺带避免阶段 5 的粒子特效被杆体缩放压扁；`OnValidate` 会检查这一点
- 新增 `flameLift`（世界单位，不随杆体缩放）用于微调火苗核心比灯芯高多少

> **教训**：凡是"设计师手填的值必须和资产的某个属性保持一致"，就是一个迟早会静默错掉的魔数。代码已经能读到那个属性时，就该由代码去写。

#### 1.3.3 区域列表允许重复（潜在 bug）✅

一个载体可能挂多个碰撞体（根节点 trigger + 杆体 collider），进同一区域会收到多次 `OnTriggerEnter`。原先 `EnterZone` 去重，导致**任一碰撞体先离开就删掉整条记录**，另一个还在区域内却没人记得 → 火会莫名"恢复"。

改为**允许重复**：列表天然成为计数器，进几次记几笔、出几次删几笔，全出完才算离开，而"后进入者优先"不需要额外代码即照旧成立。日志仍只在首次进入/彻底离开时打印。

> 当前未暴露只是因为验收路径是"离开 Deep 回到 Cave"，`Remove(Deep)` 后 `[Cave]` 恰好正确。

#### 1.3.1 顺带修掉的 bug ✅

**两条熄灭路径不一致**：硬熄灭（fuel/oxygen 归零）会 `OnDetach` 并放开载体引用，倒计时熄灭（`dyingTimer` 归零）**只改了状态**。后果是火灭之后载体的 `isLit` 仍为 true，继续在自己的 `Update` 里烧燃料、继续缩短。

修法不是给漏掉的那条补一行，而是**合并成唯一出口** `Flame.Extinguish(reason)`：记录 `lastKnownPosition` → 改状态 → `OnDetach` → 引用置 null。

同时把"熄灭后 `currentCarrier` 为 null"这个唯一的 null 窗口在 `Flame` 内部消化掉（`VisualPosition` / `Fuel` / `Oxygen` 全部安全返回，另加 `HasCarrier`），镜头与视觉层不需要各判一次空。

#### 1.4 吹火复燃 ⬜

按初稿：`Dying → Burning`（**不是回 Ember**），吹火期间**玩家无法移动**，且**额外消耗一定 fuel**。

- ⬜ `Flame` 暴露 `Blow()`：仅在 `Dying` 态生效，扣一次性 fuel，成功则置 Burning
- ⬜ 额外消耗走 `Carrier.ConsumeFuel(amount)` 这个已有入口（它收绝对量，正是为此留的）
- ⬜ **失败分支**：若额外消耗会把 fuel 扣到 0，则吹火直接导致熄灭——不做保护性截断，这是"燃料不可逆"的一部分
- ⬜ 移动锁定与解除（角色控制在阶段 3，此处先留接口 + 临时按键）

#### 1.5 阶段验收

**走一遍**：火把变短 → 火变小 → 进密闭空间火变暗 → 濒熄时吹火复燃 → 烧尽前交接成功则火延续，没接上则熄灭失败。

**做完这一步核心已可验证，剩下的都是在它上面加载体。**

---

### 阶段 2 · 镜头系统（1 天）⭐ 最高风险 ⬜

**为什么排在关卡之前**：这是全项目最独特也最容易翻车的一块。它若不成立，关卡设计的空间感前提会整个塌掉，越晚发现越贵。

- ⬜ 加权中点算法（玩家与火种之间，按距离加权）
- ⬜ 距离权重曲线：火在手上时几乎等同标准第三人称，火脱手越远越偏向火
- ⬜ FOV 自适应
- ⬜ 火脱手时的方向指示
- ⬜ 过场模式（完全锁定火，用于蒙太奇与太空关最后一帧）
- ⬜ **必须验证第三人称与锁定火种能否共存**（题目硬要求第三人称，不可牺牲）

**降级预案**（超两天即执行）：退回标准第三人称 + 火种屏幕边缘指示器，"镜头聚焦火种"降级为仅用于过场。**执行降级当天就写进取舍日志。**

---

### 阶段 3 · 角色控制（0.5 天）⬜

- ⬜ 第三人称移动，**固定速度、无奔跑**（取舍日志 2026-09-02：奔跑把移动速度与两套计时器隐式绑定，玩家读不出因果）
- ⬜ 持火 / 空手两套状态
- ⬜ 持火时被禁用的操作清单（→ 设计文档 3.3「持火状态对照表」，**边做边填，不要事后回忆**）
- ⬜ 放下火把 / 拾起火把（吹火与洞穴高潮段的前置）
- ⬜ 接上阶段 1.4 的移动锁定

---

### 阶段 4 · 石器关（2 天）⬜

**做完这一关，demo 就有了最低可交付版本。**

- ⬜ **段落一 · 教学**：三根长短不一的干木枝，最短那根刚好不够走到对岸（让玩家无痛失败一次）
  - 长度差必须**一眼可见**——它同时是本关唯一的燃料 UI
- ⬜ **段落二 · 兽群**：狼形几何体 AI（保持在火光半径外、绕圈试探、找空档突进）
  - ⬜ 火光半径从 `Flame.State` 派生，**不另设变量**
  - ⬜ 两个战斗动作：横扫驱赶 / 突刺点燃
  - ⬜ 胜利条件是撑住，不是杀光；**全程不允许杀死任何野兽**（公理 III）
  - ⬜ 代价兑现：战斗打得越久，进洞穴时火把越短
- ⬜ **段落三 · 洞穴**：湿冷（低氧区域）→ 火把加速衰减 → 柴堆前只剩余烬
  - ⬜ 投入随身可燃物 + 俯身吹火（复用 1.4，不写新逻辑）
  - ⬜ **此处不给任何文字提示**：教学期给，高潮期撤
  - ⬜ 角色倒下，身体留在原地（下一关的遗留物）
- ⬜ 燃烧状态与连锁传播（材料响应查表，表在材料侧不在 `Flame` 内）

**验收**：一个完整可玩的 5 分钟关卡，从点火到倒下不中断。

---

### 阶段 5 · 火焰特效打磨（1 天）⬜

- ⬜ 三维度参数化：尺寸（fuel）/ 浑浊度·黑烟（oxygen）/ 颜色（派生温度）
- ⬜ 闪烁（isDying）
- ⬜ `legacyColor` 系统 —— **初稿明确：核心循环跑通前不写**，本阶段才落地
- ⬜ `Flame` 加身份数据 `VisitedEras`
- ⬜ 所有火共用同一套资产验证（篝火 / 火把 / 炉膛 / 管网都是同一团火）

---

### 阶段 6 · 工业关（2.5 天）⬜

- ⬜ `PipeNetwork : Carrier` —— **它继承载体基类，工业关没有引入任何新类型**
  - ⬜ 图结构：节点 / 边 / 可燃性 / 燃烧耗时权重
  - ⬜ `Fuel` 由全图聚合，`Position` 取燃烧质心（供镜头与 `FlameVisual` 使用）
  - ⬜ 沿可燃边按权重扩张
- ⬜ 阀门：切断一条边，耗时约 3s
- ⬜ 放水：淹没区域永久不可燃，**且水会流**（可能淹到你还需要的地方）
- ⬜ 管道受热由灰变红的全局可读性（**零 HUD**）
- ⬜ 主阀：能一次性掐断全部火路 → 系统提示"这将熄灭火种" → **拒绝执行**
- ⬜ 段落三两条路径 + 后果在关卡后半段真实兑现（选 A 后燃料紧张 / 选 B 后火苗微弱）
- ⬜ 威胁触发前留一段无压力的观察窗口

**降级预案**：超时则 `PipeNetwork` 降级为**预设路径的火焰蔓延**（不做实时图搜索），完整图结构模型写进设计文档第 6 章。

---

### 阶段 7 · 太空关 + 蒙太奇（1 天）⬜

- ⬜ 密封舱载体、注氧（**在舱外操作**）、关舱门
- ⬜ 真空 = `oxygen 0` 区域，直接复用 `OxygenZone`，不新增规则
- ⬜ 最后一帧：镜头**第一次跟着火离开**，玩家角色留在地面
- ⬜ 蒙太奇 ×2（石器→工业、工业→太空）：**镜头绝对不动**，剪影几何体高速掠过 + 环境色渐变
- ⬜ 掠影结束后静止一两秒，火独自燃烧

---

### 阶段 8 · 收尾（1 天）⬜

- ⬜ **先定关键比值**：一根标准火把的燃烧时长 ÷ 走完一个典型房间的时长（决定整体紧张度，其他数值都跟着它调）
- ⬜ 三关串联 + 参数平衡
- ⬜ Build .exe（确认无报错、无缺失引用）
- ⬜ 打包源文件（7z / zip）
- ⬜ 录制试玩视频（mp4 / H.264）
- ⬜ **交付物五类分开提交**（.exe / 源文件 / 设计文档 pdf / 网络资源备注 / 视频）

---

### 阶段 9 · 设计文档（2–3 天，与开发并行）🔨

**不是最后三天的事。** 每完成一个阶段就补对应章节，趁细节还热。

| 写作时机 | 补哪一章 |
|---|---|
| 阶段 1 完成 | 第 2 章 核心机制建模（**最先写，其他章节都依赖它的定义**）、第 9 章 技术实现 |
| 阶段 2–3 完成 | 第 3 章 操作规则（含摄像机规则、持火状态对照表） |
| 阶段 4 完成 | 第 4 章 关卡设计（石器）、第 5 章 战斗设计 |
| 阶段 5 完成 | 第 8 章 美术与表现 |
| 阶段 6–7 完成 | 第 4 章补齐、第 1 章 主题与设定 |
| 全程 | 第 7 章 取舍（从 `取舍日志.md` 提炼）、第 10 章 资源出处、第 11 章 AIGC 说明 |
| 最后 | 第 0 章 概览、第 6 章 未实现设计 |

**硬要求**：
- 第 2 章参数表必须有「调整它会影响什么体验」这一列
- 第 6 章未实现部分要写得和已实现部分一样认真（统一格式：设计描述 / 玩法建模 / 数据结构 / 如何接入 / 未实现原因）
- 第 7 章至少三条"我原本设计成 X，发现 Y，改成 Z"
- ⚠️ **AIGC 直接生成内容标红，且不得超过全文 20%**（超过则整份测试无效）。简报与本规划均由 AI 协助整理，**不可整段粘贴进设计文档**

---

## 3. 关键路径与并行

```
阶段0 ──> 阶段1（火焰核心）──> 阶段2（镜头·最高风险）──> 阶段3 ──> 阶段4（石器关·最低可交付）
                                                                        │
                                                          ┌─────────────┴─────────────┐
                                                       阶段5（特效）             阶段6（工业关）
                                                          └─────────────┬─────────────┘
                                                                     阶段7 ──> 阶段8（收尾）

阶段9（设计文档）自阶段 1 起全程并行
```

**两个不可压缩的节点**：
1. **阶段 1 未完成前不进阶段 4** —— 关卡是搭在核心循环上的，核心晃动会让关卡数值全部白调。
2. **阶段 4 完成即达最低可交付线** —— 此后任何阶段超时都可以砍内容而不影响"有东西可交"。

---

## 4. 待定与需确认（做到对应阶段前必须有答案）

| # | 问题 | 挡住哪一步 | 现行临时取值 |
|---|---|---|---|
| 1 | 一根标准火把撑多久 ÷ 走完典型房间多久 | 阶段 4 关卡尺度 | `fuelCapacity=30`、`burnRate=1` → 30s |
| 2 | 各区域 oxygen 具体值 | 阶段 1.2 | 露天 10 / 密闭 5 / 深处 1.5 / 真空 0（待实测） |
| 3 | 吹火一次额外扣多少 fuel | 阶段 1.4 | 待定，建议 2–3（够疼但不必死） |
| 4 | `dyingDuration = 8s` 是否合适 | 阶段 1.5 验收 | 8s |
| 5 | 兽群数量与围攻半径 | 阶段 4 | — |
| 6 | 管网图规模（节点数）与扩张速度 | 阶段 6 | — |
| 7 | 颜色规范表色值（须与火焰橙红区分） | 阶段 0 收尾 | — |
| 8 | 蒙太奇剪影数量与节奏 | 阶段 7 | — |

> 引擎选择已确认（Unity），简报第 15 章的该条可视为关闭。

---

## 5. 风险登记

| 风险 | 影响 | 触发信号 | 应对 |
|---|---|---|---|
| 镜头方案不成立 | 空间感前提塌掉 | 阶段 2 超两天 | 降级为标准第三人称 + 边缘指示器，写进取舍日志 |
| `PipeNetwork` 实时图搜索超时 | 工业关做不完 | 阶段 6 过半仍未跑通扩张 | 降级为预设路径蔓延，图结构模型进文档第 6 章 |
| 关卡内容挤掉文档时间 | 文档分数是大头，损失更大 | 阶段 6 结束时文档不足一半 | **砍关卡内容，不砍文档**；未实现部分按第 6 章格式补写 |
| 为某一关加特例逻辑 | 破坏"三关不新增规则"的核心纪律 | 出现"这一关需要一个特殊的……" | 一律拒绝，改为新增材料或新增 `Carrier` 子类 |
| 简报与初稿再次漂移 | 代码按错的那份写 | 改动涉及状态机 / 载体 / 阈值 | 先查初稿；初稿要改由作者本人改，改完同步简报 12.0 节 |

---

## 6. 已知文档漂移（待清理）

以下是当前仓库里与初稿不一致、但**尚未修正**的地方，改到相关章节时顺手一并处理：

| 位置 | 现写法 | 应为 | 处理 |
|---|---|---|---|
| 简报 6.2 / 6.3 / 6.4 / 6.5 | `IFuelSource` 接口 | `Carrier` 抽象基类 | ⬜ 待改（已在 12.0 节声明以初稿为准） |
| 简报 5.2 / 6.6 | `fuelRatio < 0.25` 等归一化阈值 | 绝对值 `fuel < 10` 等 | ⬜ 待改 |
| 简报 6.2 `Flame.Tick` | `_current.ConsumeFuel(dt)` | 载体自己在 `Update` 扣 | ⬜ 待改 |
| 简报 6.2 | "不需要额外建一个 Runner 类" | 实际有 `FlameRunner`（只驱动、不判定） | ⬜ 待改 |
| 简报 5.2 | "Dying 期间玩家可移动" | 初稿：吹火时无法移动（Dying 期间移动本身不禁止，**吹火动作期间禁止**） | ⬜ 待确认措辞 |
| 简报 6.2 / 5.3 | `FlameVFX.cs` | `FlameVisual.cs` | ⬜ 待改 |
| `取舍日志.md` 2026-09-02 第三条 | "载体保持为接口 `IFuelSource`" | 已改为 `Carrier` 抽象基类 | ⬜ 需补一条新日志说明为何又改回基类（**这条本身就是设计文档第 7 章的好素材**） |

> **不要在本文档里"顺手改掉"这些。** 规则的家在初稿，措辞的家在简报。这里只登记。
