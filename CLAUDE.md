# 《火种》Project Ember — 开发约定

> 新会话请先读完本文件。它规定**怎么写代码、怎么写注释、怎么交付**，不规定玩法规则——玩法规则在设计文档里。
> 项目性质：miHoYo 2027 秋招 · 技术策划岗笔试。主题「传递」。Unity `6000.0.76f1`（URP），交付前不升级引擎。

---

## 1. 文档权威顺序

```
Assets/Docs/Design/设计文档初稿.md      ← 最高权威，玩法规则的家
Assets/Docs/Design/火种_项目开发简报.md  ← 把初稿展开成可实现的形式
Assets/Docs/Design/开发规划.md          ← 把阶段展开成可执行的任务 + 当前进度
本文件 CLAUDE.md                        ← 代码风格与协作约定
代码
```

**冲突时一律以更靠前者为准。** 下级文档负责展开上级，**不得反过来修改上级的规则**。

- 简报第 5、6 章有几处写于初稿定稿之前（`IFuelSource` 接口、归一化阈值、`FlameVFX` 命名），**已作废，以初稿为准**。`开发规划.md` 第 6 节登记了全部已知漂移。
- 发现规则冲突时：**先查初稿；初稿没写的，记进 `开发规划.md` 的待定表，问过作者再动手。不要自己发明规则。**

其他文档：`Assets/Docs/取舍日志.md`（设计文档第 7 章的唯一素材来源）、`Assets/Docs/材质颜色.md`（颜色规范表）。

---

## 2. 最重要的一条纪律

> **本项目只有三条公理，关卡之间不新增任何规则，只新增材料。**

任何"为了这一关加个特殊逻辑"的提议都应被拒绝，改为新增材料或新增 `Carrier` 子类。

这条纪律在代码层同样成立：工业关的管网不是新类型，它只是一个更复杂的 `Carrier`。**规则从层级里漏出来，和从代码里漏出来一样算漏**——比如把 trigger 挂到会被缩放的节点上，就等于凭空造出一条"燃料越少越难传火"的隐性规则。

---

## 3. 架构约束（改动前必读）

一句话概括整套架构：

> **火是一个没有 Transform 的逻辑单例，寄居在载体上；燃料属于载体，氧气属于区域，视觉层只读不写。**

由此推出五条硬约束：

### 3.1 数据放对地方，就不用写搬运代码

`Flame.Fuel` 读的是 `currentCarrier.Fuel`，`Flame.Oxygen` 读的是载体转告的区域值。所以火交接载体时**零同步代码**。

添加新功能时先问："这个数据的家在哪？"放对了，大半的同步逻辑就不用写。

### 3.2 只读层就是只读的

`FlameVisual`、`FlameDebugHUD`、`FlameOffscreenIndicator` 只从 `Flame` / `Carrier` 读值，**不写回、不注册回调、不让 Flame 为它们加事件**。

`FlameDebugHUD` 的事件日志是自己每帧比对上一帧 diff 出来的，`Flame` 侧一行代码都没为它改——这是刻意的。

**自测方法：禁用该组件，游戏行为必须一模一样。** 一旦只读层里出现影响判定的代码，架构就漏了。

### 3.3 同一件事只能有一个出口

熄灭曾经有两条路径（硬熄灭 / 倒计时），各写各的收尾，倒计时那条漏了 `OnDetach`，导致火灭之后载体继续烧燃料。

**修法不是给漏掉的那条补一行，是合并成唯一出口** `Flame.Extinguish(reason)`。以后再加第三条熄灭路径也不会漏。

### 3.4 null 窗口在内部消化，不让外面各判一次

`currentCarrier` 唯一为 null 的情况是火已熄灭。这个窗口在 `Flame` 内部处理掉（`VisualPosition` / `Fuel` / `Oxygen` 全部安全返回），**镜头、视觉层、调试面板都不该再判一次空**。

### 3.5 fuel 只由载体自己改

`Carrier.Fuel` 是 `{ get; protected set; }`——这条规则是编译器约束，不是注释。`Flame` 想写也写不进来。

---

## 4. 代码风格

### 4.1 基本约定

- 语言：C#，中文注释。标识符用英文
- 命名：类型/公开成员 `PascalCase`，私有字段 `camelCase`（不加 `_` 前缀），常量 `PascalCase`
- 序列化字段用 `[SerializeField] private`，不要用 public 字段
- **运行时值不要序列化**：用属性（`public float Fuel { get; protected set; }`），不要用公开字段。字段会被存进场景，Inspector 里显示成上次存盘的值，看着像 bug
- 阈值等只读事实写成 `public const`，视觉层和调试面板要读它们
- 用 `[Header("...")]` 给 Inspector 分组
- 分节用 `// ── 名字 ──────────────────` 划开

### 4.2 注释规范（本项目最看重的一条）

**注释写"为什么"，不写"是什么"。** 代码已经说了它在做什么。

```csharp
// ✗ 不要
// 把 fuel 减去 amount
Fuel = Mathf.Max(0f, Fuel - amount);

// ✓ 要
// 只负责钳到 0，不在这里销毁自己：
// 此刻 Flame 还持有 currentCarrier 引用，销毁会让它下一帧读到悬空引用。
// 销毁统一放在 OnDetach——那时 Flame 已经放手了。
Fuel = Mathf.Max(0f, Fuel - amount);
```

具体要求：

1. **每个类有 XML 文档注释**，说明职责、期望的场景层级、以及关键的装配约束
2. **重大设计决策用 `【】` 起标题写成整段**，并且**写下被否决的方案和否决理由**：

   ```csharp
   /// 【为什么是区域检测载体，而不是载体检测区域】
   /// 1. Torch 已经有自己的 OnTriggerEnter（传火用）。若在 Carrier 基类里
   ///    再写一个，Unity 只会调子类那个，基类的检测会静默失效。
   /// 2. 火交接到新载体时，新载体早就待在房间里了，不会再触发一次 Enter。
   ```

   被否决的方案是最有价值的注释——它挡住了下一个人重走弯路。

3. **顺序有意义的地方必须写明**：

   ```csharp
   // 顺序要紧：吹火要先扣掉这一帧的燃料，再让状态机看当前值。
   // 这样"吹到燃料归零"就落进 CheckIfChangeState 的硬规则里，
   // 不需要在吹火逻辑里再写一次熄灭判定
   ```

4. **装配约束写进代码，不要只写在聊天里。** 那种"照做不会出错、违反了也不报错"的规则，只存在于对话记录里必然会丢：

   ```csharp
   /// 【Collider 和 Rigidbody 必须留在根节点，不能挪到 Stick 上】
   /// 1. Stick 的 localScale 每帧被改，碰撞体形状是按缩放烘出来的……
   ```

5. **纯表现层的方法要注明"删掉也能跑"**，这是在标记架构边界

### 4.3 防呆

- `OnValidate` 里检查装配错误并 `Debug.LogWarning`（缺引用、Collider 没勾 Is Trigger、挂点挂错父节点……）
- `Reset()` 里给一组合理默认值
- 看不见的中间计算结果用 `OnDrawGizmos` 画出来。`Torch` 把"代码算出的杆顶"画成白球——这类值错了只能靠手算去查，画出来就一眼可知

---

## 5. Unity 特有约束

| 事项 | 约束 |
|---|---|
| 输入 | `activeInputHandler: 1`，**只有新 Input System**。`Input.GetKey` 会抛异常，必须用 `UnityEngine.InputSystem`（`Keyboard.current` 可能为 null，要判） |
| Update 顺序 | Unity 不保证脚本间的 `Update` 顺序。`FlameRunner` 因此放在 `LateUpdate`，等所有载体扣完燃料再判状态 |
| 刚体与缩放 | **刚体和碰撞体不能待在每帧被缩放的节点上**，碰撞形状会每帧重烘 |
| Unity 伪 null | 被销毁的对象 `== null` 为真但引用还在。集合里要跳过，不要假设非空 |
| 网格尺寸 | 从 `mesh.bounds.max.y` / `min.y` 读，不要手填。Cube 顶面在 `0.5`，Cylinder 在 `1.0`——手填必然错一倍 |
| 触发器 | 两个 trigger 之间至少一方要有 Rigidbody。一个载体可能有多个碰撞体，进出区域会收到多次回调 |

---

## 6. 工作流

### 6.1 交付方式（重要）

**代码直接写进 `Assets/Scripts/**` 文件里**（用 Write / Edit）。

**回复里只给伪代码 + 设计理由**，不要粘贴完整的可编译代码——那等于要作者手抄一遍，既慢又容易抄错。他需要的是"为什么这么写"和结构轮廓。

回复结构：

1. 改了哪些文件（表格，一行一个改动摘要）
2. 关键设计决策 + 被否决的方案
3. 伪代码说明控制流
4. **Unity 编辑器侧的装配步骤**（作者要手动做的）
5. **验收清单**：具体操作 → 期望现象，一行一条

### 6.2 阶段推进

按 `开发规划.md` 的阶段走，一步一验收。**不要一次做完多个步骤**——每步之间作者要在 Unity 里手动装配和验证。

验收清单要写成"操作 → 期望"的表格，并明确指出**哪一条是这一步真正要验的东西**（其他条往往是顺带）。

### 6.3 改了方案就记取舍日志

去 `Assets/Docs/取舍日志.md` 记一条，格式：**原方案 / 实际问题 / 当前决定 / 代价 / 后续验证**。

这是设计文档第 7 章的唯一素材来源。**当场记，事后补必然丢细节。**

### 6.4 读场景状态

`.unity` 是 YAML，可以直接读来核对 Inspector 里的实际数值——比让作者截图快。但注意**作者可能还没存盘**，读之前先确认。

### 6.5 动手写之前，先问"有没有现成的官方包"

镜头写过一遍手写版才换成 Cinemachine，白扔一天（取舍日志 2026-09-05）。教训有两条：

1. **手感参数要落在 Inspector 里，不要锁在代码里。**
   判据是一句话：**作者能不能不读 C# 就把它调好？** 跟随距离、阻尼、俯仰范围、过渡曲线、
   音量、粒子——这类东西一旦只能靠改代码来调，交付质量就已经掉了一档，
   哪怕它跑起来完全正确。**"能跑"和"作者能继续改"是两件事，后者也是交付物。**
2. **能用官方包做的，先摆出来问，不要默认自己写。**
   "它不在 `manifest.json` 里"**不是**否决理由——装一个包只要一行。
   CLAUDE.md 4.2 要求写下被否决的方案，那就必须包含**评审最可能问的那一个**
   （镜头 → Cinemachine，UI → UI Toolkit，寻路 → NavMesh，输入 → Input Actions 资产）。
   漏掉它就等于没做这个权衡。

反过来也成立：自己写并非总是错的。判断顺序是**先摆出现成方案，权衡后再决定**，
而不是跳过权衡直接动手。这次真正的错误是漏掉了权衡这一步，不是最后选了哪边。

### 6.6 不要从枚举的完整性反推需求

镜头一度有三种模式，其中 `Hold`（蒙太奇用的固定机位）**是凭空造出来的**：
造它的理由是"锁定 / 冻结看起来该配成一对"，不是因为哪一关需要它。
而蒙太奇里火本来就不动，锚点是火的那个模式自然就不动——
**要的效果早就是现有模式的自然结果了**（取舍日志 2026-09-05 同日修正）。

加一个枚举值 / 一种模式 / 一个状态之前，先问一句：

> **设计文档或简报里，哪一段点名要它？**

答不上来就不要加。这条和第 2 节那条纪律是同一件事的两面：
「关卡之间不新增规则，只新增材料」在代码里的对应物，就是
**不要新增模式，只新增数据**——工业关要"拉远看全厂"，那是**多一台 vcam**，
不是多一种模式；新载体是多一个 `Carrier` 子类，不是多一条状态机分支。

判断长歪了没有，看**加一个新东西要不要改已有的代码**：
要改，说明这个结构在用枚举硬编码本该是数据的东西。

---

## 7. 脚手架约定

调试和临时代码**必须能整块拔掉**：

| 文件 | 用途 | 何时删 |
|---|---|---|
| `FlameDebugHUD.cs` | IMGUI 调试面板（F1 开关） | 交付前 |
| `TempBlowInput.cs` | 临时吹火输入（空格） | 阶段 3 接入角色控制器时 |
| `TempPlayerStandIn.cs` | 临时玩家：WASD 移动 / G 放下火把 / C 切镜头模式 | 阶段 3 接入角色控制器时 |

规则：

- 独立文件，不要混进正式代码。混进去就删不干净
- 类注释开头写明 `⚠️ 临时脚手架，X 时删掉整个文件`
- 调试工具用 IMGUI（`OnGUI`），不要用 Canvas / UI Toolkit——不需要任何资产和预制体，删的时候删一个文件
- `Debug.Log` 走 `Flame.ConsoleLog` 开关，默认关

---

## 8. 已经踩过的坑

写新代码前扫一眼，别重复踩：

1. **Unity 消息覆盖**：基类和子类都声明 `OnTriggerEnter`，Unity 只调子类那个，基类**静默失效**、不报错
2. **手填的魔数必须和资产属性同步**：`FlamePosition.localPosition.y` 要等于网格顶面高度，按 Cylinder 填了 `1.0` 而实际是 Cube（`0.5`），结果火苗以两倍速下降。**代码能读到那个属性，就该由代码写**
3. **去重导致的引用计数错误**：一个载体多个碰撞体，进区域收到多次 Enter。若 Enter 时去重，任一碰撞体先离开就删掉整条记录 → 火莫名恢复。**改为允许重复，列表天然成为计数器**
4. **两条路径做同一件事，其中一条漏了收尾**（见 3.3）
5. **公开字段会被序列化**：`public float Fuel;` 被存进场景，Inspector 显示 `0`，看着像 bug
6. **日志不带成因就分不出病因**：四个状态由两个独立变量驱动，`→ Extinguished` 不写明是燃料还是氧气，调参时会反复卡住
7. **物理查询默认会打到 trigger**：场上所有 `OxygenZone` 都是 trigger 且罩住整个房间。镜头的防穿墙检测若不排除它们，玩家一进洞穴镜头就被区域体积顶到脸上——又一条没人设计过的隐性规则。**凡是射线 / 球检测，先问一句"它会不会打到区域体积"**
   - 排除方式优先用 **Layer**（`Zone` / `Player`），不要只靠 `QueryTriggerInteraction.Ignore` 那个参数：Layer 在 Inspector 里看得见、第三方组件（Cinemachine Deoccluder）也认，而且玩家将来挂上 CharacterController 之后依然成立
8. **`WorldToScreenPoint` 在相机背后返回镜像坐标**：`z < 0` 时它照样给出一组看着正常的 x/y，那是投影到镜头**后方**的虚像。直接拿来算方向，屏幕边缘的指示箭头会稳定指反，而且不报错。要绕屏幕中心镜像回来

---

## 9. 交付物提醒

题目要求五类**分开提交**：`.exe` / 打包源文件 / 设计文档（pdf 优先）/ 网络资源备注 / 试玩视频（可选）。

⚠️ **AIGC 直接生成的内容需标红，超过全文 20% 该份测试视为无效。** 本文件与各设计文档均由 AI 协助整理，**不可整段粘贴进最终设计文档**，正文需作者自行撰写。
